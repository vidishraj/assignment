# This environment sets NODE_ENV=production globally, which makes `npm install`
# omit devDependencies (TypeScript, tsx, types). Force them so the project is
# buildable/testable out of the box.
include=dev
